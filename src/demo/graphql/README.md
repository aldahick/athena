I generally would not recommend placing this directory within `src`, but have done so in this repository to avoid confusion (since it applies to `demo`, not the core library at the root of the repo).
